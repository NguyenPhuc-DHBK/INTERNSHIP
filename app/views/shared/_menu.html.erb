 <div id="menu" class="container-fluid">
      <div class="menu-item">
          <div class="menu-item-logo"> 
            <a href="#" style="font-size: 20px; font-weight: bold; color: white; text-decoration: none;">Fotobook</a>
          </div>
          <div class="menu-item-form">
            <form action="#">
                <input type="text" placeholder="Search Photo / Album">
            </form>
          </div>
      </div>
        <div class="menu-item">
            <% if user_signed_in? %>
            <a href="#" class="user-info d-flex">
                <div class="logo-user"><%= current_user.firstname[0] %><%= current_user.lastname[0] %></div>
                <div class="name-user"><%= current_user.firstname %> <%= current_user.lastname %> </div>
            </a>
            <% end %>
            <% if user_signed_in? %>
              <%= link_to "Logout", destroy_user_session_path, :method => :delete , class: "login-logout" %>
            <% else %>
              <%= link_to "Login", new_user_session_path, class: "login-logout" %>
            <% end %>
        </div>
  </div>